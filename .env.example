# Copy this file to .env.local and fill in both values (Supabase > Project Settings > API).
# The publishable key is meant to be public. Never put a service_role or secret key here.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
